main = "/worker.ts"
compatibility_date = "2024-05-12"
compatibility_flags = ["nodejs_compat"]

[[durable_objects.bindings]]
name = "Stateful"
class_name = "Stateful"

 [[migrations]]
tag = "v1" # Should be unique for each entry
new_classes = ["Stateful"]